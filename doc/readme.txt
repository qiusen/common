通用工具



lucene库增量更新设计：
使用lucene时，写两份索引库文件，即两个目录下，
一分是直接在文件上写，另一份是单独写文件进行增量处理。
使用哪个库文件进行查询，根据当时时间是一年中的第几天%2，结果为0或1进行区分，
切换时把有两个库文件的索引目录中的索引进行合并。
这样可保索引增量更新无问题，同时可以在配置文件中给出-1、0、1三个值，
-1时自动切换；0时只使用当前时间为当年偶数的索引库，1时同理。